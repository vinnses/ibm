# W049 — Human review question

## HR-W049-001 — GitHub false-positive classification for a public Mapbox token

- **Related gap:** GitHub push protection rejects local `main` and descendants W032-W049 because the preserved public page capture `dados/cursos-ia-ibm/w044/fontes/curriculos/pucpr_ai_aplicada_ead.html:4257` contains a Mapbox `pk` access token.
- **Why agent work is insufficient:** Preserved official Mapbox documentation distinguishes public browser tokens (`pk`) from secret tokens (`sk`) and says public tokens are intended for client-side exposure. The source appears to contain the public kind. However, only the repository owner can submit the false-positive classification through the authenticated GitHub security UI; no browser session is available in this runtime. Captures and hashes: `governance/sources/W049-public-token-classification/manifest.csv`.
- **Human action/question:** In the GitHub repository's push-protection review for this occurrence, classify it as a false positive if the owner agrees that this preserved public page's `pk` token is not a confidential credential. Do not choose a bypass for a secret token. After classification, tell the primary session so it can retry ordinary non-forced pushes.
- **Required response provenance:** Confirmation that the GitHub UI marked this specific occurrence as a false positive; do not send the token value or the owner-specific unblock URL.
- **Gate consequence:** Pending classification blocks pushes for local `main` and 18 local branch names W032-W049. The local deployment and commits remain intact; matching older branches and four remote-only branches are preserved. No rebasing or force-push is planned.
- **Status:** Pending user/owner action.
