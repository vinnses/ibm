# Work Specification — W008: P0 preservation and consistency closure

- Objective: close every P0 preservation and consistency exception so a clean checkout can reproduce existing source lookups, or produce a blocked handoff that identifies the inaccessible source or indispensable human storage decision without weakening the evidence rules.
- Branch: `work/w008-p0-preservation`
- Commit base: `478dbe11a14ff2451f78b43f04794d79428492f6`
- Inputs: `administracao/dados/fontes.csv`; preserved INEP pages and MD5 manifests; official INEP download URLs recorded in the manifest; `administracao/dados/README.md`; `administracao/dados/AUDITORIA.md`; `administracao/mec/2026/matriz-validacao-alegacoes.md`; `assembleia/subsidio-factual-preliminar.md`; local source manifests; governance decisions and specifications.
- In scope: retrieve and verify the 11 exact INEP packages; preserve the exact extracted spreadsheets used by W006; replace temporary absolute paths with stable repository-relative paths; document and validate the repository-backed storage mechanism; refresh the assembly factual brief from consolidated evidence while preserving fact/allegation/gap distinctions; decide and document whether the global catalog is comprehensive or intentionally selective; run an independent review; integrate only on an approving verdict.
- Out of scope: new P1 documentary research; new administrative series; curricular comparison or quality judgment; proposal design; changes to original captured bytes; treating a failed search or inaccessible source as evidence of nonexistence.
- Deliverables: stable INEP package and spreadsheet paths under `administracao/dados/inep/fontes/`; updated `administracao/dados/fontes.csv`, preservation documentation, validator coverage, and factual assembly brief; a durable catalog decision; `governance/reviews/W008-p0-preservation-consistency.md`; `governance/handoffs/W008-p0-preservation-consistency.md`.
- Method: download only from the official URLs already recorded; compare package SHA-256 and official spreadsheet MD5 before use; retain package and spreadsheet bytes unchanged; record repository-relative paths, byte sizes, provenance, retrieval date, document type, evidentiary purpose/status where the current schema permits; keep transformations and derived extracts separate; derive the assembly update only from preserved repository evidence; do not add P1 findings. Use a repository-backed versioned mechanism that a clean checkout can materialize. If no such mechanism can be configured or exercised, stop with the exception unresolved.
- Acceptance criteria: all 11 package records and all 11 exact spreadsheet records resolve to preserved bytes from a clean checkout; recorded SHA-256, byte size, and official MD5 checks pass; W006 extraction and summary outputs regenerate byte-for-byte; the assembly brief reflects the consolidated administrative data and validation matrix without normative claims; the catalog scope is explicit and non-duplicative; `python scripts/validate_repository.py` reports zero errors and zero unresolved preservation warnings; review verdict is approving before integration.
- Risks and uncertainty: official files may have changed or become inaccessible; Git LFS is not installed locally and remote LFS availability is unknown; ordinary Git/GitHub object limits may make direct storage unsuitable; the original scratch XLSX paths no longer exist, so identity must be established through the preserved official MD5 manifests and exact matching package bytes; an external storage choice may require a human decision.
- Validation: `python scripts/validate_repository.py`; SHA-256 verification for every package and spreadsheet; MD5 comparison against the preserved official manifests; ZIP integrity tests; W006 extraction and summary regeneration in a temporary directory with byte comparisons; Markdown link check through the repository validator; `git diff --check` with preserved-source whitespace exceptions documented; clean-checkout materialization test; independent review against `governance/specs/REVIEW.md`.
